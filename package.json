{
  "name": "docs",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "lint": "node readFiles.js && prettier --w . --config .prettierrc"
  },
  "devDependencies": {
    "prettier": "^2.4.1",
    "vitepress": "^0.20.0"
  }
}
